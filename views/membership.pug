extends layout 

block content 

    header
        h1=title 

    div.window
        p Members can view the authors and timestamps of each message.

        p Type in secret password:

        form(action="" method='POST')
            div.form-group
                input.form-control(
                    type='text',
                    name='membershipPassword',
                )
            div.button-container
                button(type='submit') Join...
                a(href="/login") Skip for now! 
        
        if errors 
            each error in errors 
                li !{error.msg}