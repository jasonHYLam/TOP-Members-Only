extends layout 

block content 

    h1=title 

    p Admins can delete posts 

    p Enter password:

    form(action="" method='POST')
        div.form-group
            input.form-control(
                type='text',
                name='adminPassword',
            )
        button(type='submit') Submit