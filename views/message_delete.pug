extends layout 

block content 

    h1 Delete Message 

    p Are you sure you want to delete this message? This action cannot be undone.


    form(action="" method='DELETE') 
        input(
            type='hidden',
            required='true',
            value=message._id,
        )
        button(type='submit') Yeah, delete
        button(href='/home') No, go back